// initial variables
var lisaHP = 180;
var dennyHP = 120;
var johnnyHP = 100;
var markHP = 80;

// update HP variables
$("#lisa-hp").html(lisaHP);
$("#denny-hp").html(dennyHP);
$("#johnny-hp").html(johnnyHP);
$("#mark-hp").html(markHP);

// dynamically create the characters
